When running the website locally, I want a way to be able to simulate that we're either live or not live on Twitch. The most
basic thing would probably be a command line parameter to the web server start. even better would be an admin page. So we
can simulate what happens if somebody's on the page and then we go live.
